language: node_js
node_js:
  - "node"

cache:
  directories:
    - node_modules

install:
  - npm install

script:
  - npm run lint
  - npm run build

before_deploy:
  - rm .gitignore
  - ls
  - cd lib
  - ls
  - cd ../

deploy:
  provider: npm
  email: $email
  api_key: $api_key
  on:
    branches: master

before_deploy:
  - ls
  - cd lib
  - ls
  - cd ../

branches:
  only:
    - master
