language: node_js
node_js:
  - "node"

dist: trusty
sudo: required

cache:
  directories:
    - node_modules

install:
  - npm install

script:
  - npm run lint
  - npm run build

before_deploy:
  - rm .gitignore
  - cat .gitignore
  - ls
  - cd lib
  - ls
  - cd ../

deploy:
  provider: npm
  email: $email
  api_key: $api_key
  on:
    branches: master

after_deploy:
  - cat .gitignore
  - ls
  - cd lib
  - ls
  - cd ../

branches:
  only:
    - master
